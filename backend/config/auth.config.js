// To store the string of secret key needed to encode and decode token. 
// This required by jsonwebtoken's verify() and sign() function. 

module.exports = {
    secret: "the-key-to-erebor"
  };